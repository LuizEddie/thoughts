<div class="thought-form-container">
    <a href="/thoughts/dashboard">Voltar</a>
    <h1>Editando um pensamento:</h1>
    <form action="/thoughts/edit" method="POST">
        <input type="hidden" name="id" value="{{thought.id}}">
        <div class="form-control">
            <label for="title">Titulo</label>
            <input type="text" name="title" id="title" placeholder="Digite o seu pensamento..." value="{{thought.title}}">
        </div>
        <button type="submit" class="btn">Alterar Pensamento</button>
    </form>
</div>